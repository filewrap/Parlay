name: Rooms authoritative group integration

on:
  push:
    paths:
      - .github/workflows/rooms-authority-fix.yml

permissions:
  contents: write

jobs:
  fix-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Apply scoped service, tests, and docs changes
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("src/parlay/rooms/service.py")
          text = path.read_text()

          def section(start, end, replacement):
              global text
              left = text.index(start)
              right = text.index(end, left)
              text = text[:left] + replacement.rstrip() + "\n\n" + text[right:]

          section(
              "    async def publish_playback(",
              "    async def join(",
              '''    async def set_recovering(self, chat_id: int, reason: str) -> None:
              """Mark an active group room as recovering after a transient media failure."""
              room_id = await asyncio.to_thread(self._group_id, chat_id)
              if not room_id:
                  return
              async with self._lock(room_id):
                  row = await asyncio.to_thread(self._load, room_id)
                  self._active(row)
                  reason = str(reason)[:200]
                  if row["state"] == "recovering" and row["end_reason"] == reason:
                      return
                  revision = row["revision"] + 1
                  await asyncio.to_thread(
                      self._save_status, room_id, revision, self._data(row), "recovering", reason
                  )
                  output = await self._snapshot_for(self._replace(row, revision, self._data(row), "recovering"), row["owner_id"])
              self._publish(room_id, output)

          async def publish_playback(self, chat_id: int, snapshot: dict) -> None:
              """Publish an actual registry snapshot and mark a recovering group active."""
              room_id = await asyncio.to_thread(self._group_id, chat_id)
              if not room_id:
                  return
              actual = self._clean_playback(snapshot)
              async with self._lock(room_id):
                  row = await asyncio.to_thread(self._load, room_id)
                  self._active(row)
                  data = self._data(row)
                  duplicate = self._playback_anchor(data["playback"]) == self._playback_anchor(actual)
                  if duplicate and row["state"] == "active":
                      return
                  data["playback"] = actual
                  revision = row["revision"] + 1
                  await asyncio.to_thread(
                      self._save_status, room_id, revision, data, "active", None
                  )
                  output = await self._snapshot_for(
                      self._replace(row, revision, data, "active"), row["owner_id"]
                  )
              self._publish(room_id, output)'''
          )

          section(
              "    async def snapshot(",
              "    async def action(",
              '''    async def snapshot(self, room_id: str, user_id: int) -> dict:
              row = await asyncio.to_thread(self._load, room_id)
              self._active(row)
              data = self._data(row)
              if not any(m["user_id"] == user_id for m in data["members"]):
                  raise RoomError("not_admitted", "Join the room before accessing it", 403)
              if user_id in data["kicked"]:
                  raise RoomError("access_revoked", "Room access was revoked", 403)
              if row["kind"] == "group" and not await self._check(
                  self.member, user_id, row["chat_id"]
              ):
                  raise RoomError("not_group_member", "Telegram group membership is required", 403)
              return await self._snapshot_for(row, user_id)'''
          )

          section(
              "    async def action(",
              "    async def _apply(",
              '''    async def action(
              self,
              room_id: str,
              user_id: int,
              action_id: str,
              expected_revision: int,
              action: str,
              payload: dict,
          ) -> dict:
              if not isinstance(action_id, str) or not 1 <= len(action_id) <= 128:
                  raise RoomError("invalid_action_id", "action_id is required")
              if not isinstance(payload, dict):
                  raise RoomError("invalid_payload", "payload must be an object")
              digest = hashlib.sha256(
                  json.dumps(
                      [expected_revision, action, payload], sort_keys=True, separators=(",", ":")
                  ).encode()
              ).hexdigest()
              notify: tuple[int, str, int] | None = None
              async with self._lock(room_id):
                  row = await asyncio.to_thread(self._load, room_id)
                  self._active(row)
                  data = self._data(row)
                  member = next((m for m in data["members"] if m["user_id"] == user_id), None)
                  reentry_request = action == "request_reentry" and user_id in data["kicked"] and not member
                  if row["kind"] == "group" and not await self._check(
                      self.member, user_id, row["chat_id"]
                  ):
                      raise RoomError("not_group_member", "Telegram group membership is required", 403)
                  if not member and reentry_request:
                      member = {"user_id": user_id, "role": "participant"}
                  if not member or (user_id in data["kicked"] and not reentry_request):
                      raise RoomError("access_revoked", "Room access is not active", 403)

                  replay = await asyncio.to_thread(self._replay, room_id, user_id, action_id)
                  if replay:
                      if not hmac.compare_digest(replay["digest"], digest):
                          raise RoomError(
                              "action_id_reused", "action_id was already used for another request", 409
                          )
                      return json.loads(replay["snapshot_json"])
                  if row["revision"] != expected_revision and not reentry_request:
                      raise RoomError(
                          "stale_revision",
                          "Room state changed. Refresh and retry.",
                          409,
                          await self._snapshot_for(row, user_id),
                      )

                  resolved = None
                  if action in {"queue_add", "force_play"}:
                      query = payload.get("query")
                      if not isinstance(query, str) or not query.strip() or len(query) > 500:
                          raise RoomError("invalid_query", "query must be a non-empty string")
                      if not self.search:
                          raise RoomError("search_unsupported", "Media search is not configured", 501)
                      tracks = await self.search(query.strip())
                      if not tracks:
                          raise RoomError("track_not_found", "No playable track was found", 404)
                      resolved = self._clean_track(tracks[0])

                  changed, notify, playback_call = await self._apply(
                      row, data, member, action, payload, resolved
                  )
                  if playback_call:
                      if not self.playback:
                          raise RoomError(
                              "playback_unavailable", "Group playback is not configured", 503
                          )
                      actual = await self.playback(*playback_call)
                      if not actual:
                          raise RoomError(
                              "playback_unavailable", "Group playback did not confirm the action", 503
                          )
                      data["playback"] = self._clean_playback(actual)

                  revision = row["revision"] + (1 if changed else 0)
                  state = "active" if playback_call else data.get("force_state", row["state"])
                  new_row = self._replace(row, revision, data, state)
                  if reentry_request:
                      output = {"status": "pending"}
                      publication = await self._snapshot_for(new_row, row["owner_id"])
                  else:
                      output = await self._snapshot_for(new_row, user_id)
                      publication = output
                  await asyncio.to_thread(
                      self._commit_action,
                      room_id,
                      revision,
                      data,
                      user_id,
                      action_id,
                      digest,
                      output,
                      changed,
                      state,
                  )
              if notify and self.on_reentry:
                  reentry_result = self.on_reentry(*notify)
                  if inspect.isawaitable(reentry_result):
                      await reentry_result
              self._publish(room_id, publication)
              return output'''
          )

          section(
              "    async def _apply(",
              "    def subscribe(",
              '''    async def _apply(self, row, data, actor, action, payload, resolved):
              uid, role = actor["user_id"], actor["role"]
              personal_owner = row["kind"] == "personal" and uid == row["owner_id"]
              moderator = role == "moderator"
              settings = data["settings"]
              authority = personal_owner
              if row["kind"] == "group":
                  authority = await self._check(self.authority, uid, row["chat_id"])
              privileged = authority if row["kind"] == "group" else personal_owner
              notify = playback_call = None
              if action == "settings":
                  if not privileged:
                      raise RoomError("owner_lock", "Only the room authority can change settings", 403)
                  allowed = {
                      "capacity", "password", "owner_lock", "queue_all", "theme", "tv_size", "duration"
                  }
                  if set(payload) - allowed:
                      raise RoomError("invalid_settings", "Unknown setting")
                  if "capacity" in payload:
                      cap = payload["capacity"]
                      if isinstance(cap, bool) or not isinstance(cap, int) or not 2 <= cap <= 15:
                          raise RoomError("invalid_capacity", "capacity must be from 2 to 15")
                      if cap < len(data["members"]):
                          raise RoomError("capacity_below_members", "capacity is below current membership")
                      settings["capacity"] = cap
                  if "password" in payload:
                      if row["kind"] != "personal":
                          raise RoomError("unsupported_setting", "Group rooms do not use room passwords")
                      password = payload["password"]
                      if not isinstance(password, str) or len(password) > 128:
                          raise RoomError("invalid_password", "password must be at most 128 characters")
                      data["password_hash"] = self._hash_password(password) if password else None
                  for key in ("owner_lock", "queue_all"):
                      if key in payload:
                          if not isinstance(payload[key], bool):
                              raise RoomError("invalid_settings", f"{key} must be boolean")
                          settings[key] = payload[key]
                  for key in ("theme", "tv_size"):
                      if key in payload:
                          if not isinstance(payload[key], str) or len(payload[key]) > 64:
                              raise RoomError("invalid_settings", f"{key} must be a short string")
                          settings[key] = payload[key]
                  if "duration" in payload:
                      if row["kind"] != "personal":
                          raise RoomError("unsupported_setting", "Group rooms do not expire by duration")
                      duration = payload["duration"]
                      if isinstance(duration, bool) or not isinstance(duration, int) or not 300 <= duration <= 86400:
                          raise RoomError("invalid_duration", "duration must be from 300 to 86400 seconds")
                      data["expires_override"] = row["created_at"] + duration
              elif action in {"queue_add", "force_play"}:
                  if not (privileged or moderator or settings["queue_all"]):
                      raise RoomError("queue_forbidden", "Queue changes are restricted", 403)
                  if settings["owner_lock"] and not personal_owner and row["kind"] == "personal":
                      raise RoomError("owner_lock", "Owner Lock is enabled", 403)
                  if row["kind"] == "group":
                      playback_call = (row["chat_id"], action, {"track": resolved})
                  elif action == "queue_add" and data["playback"]["track"]:
                      data["playback"]["queue"].append(resolved)
                  else:
                      data["playback"].update({
                          "track": resolved, "status": "playing", "position_seconds": 0.0,
                          "server_time": time.time(),
                      })
              elif action in {"pause", "resume", "skip"}:
                  if not (privileged or moderator or (settings["queue_all"] and not settings["owner_lock"])):
                      raise RoomError("control_forbidden", "Playback controls are restricted", 403)
                  if row["kind"] == "group":
                      playback_call = (row["chat_id"], action, {})
                  else:
                      pb = self._timeline(data["playback"])
                      if action == "pause":
                          pb["status"] = "paused"
                      elif action == "resume" and pb["track"]:
                          pb["status"] = "playing"
                          pb["server_time"] = time.time()
                      else:
                          pb["track"] = pb["queue"].pop(0) if pb["queue"] else None
                          pb["status"] = "playing" if pb["track"] else "idle"
                          pb["position_seconds"], pb["server_time"] = 0.0, time.time()
                      data["playback"] = pb
              elif action == "kick":
                  target = self._target(payload)
                  target_member = next((item for item in data["members"] if item["user_id"] == target), None)
                  target_authority = row["kind"] == "group" and await self._check(
                      self.authority, target, row["chat_id"]
                  )
                  protected = moderator and not privileged and target_member is not None and (
                      target_member["role"] in {"owner", "moderator"} or target_authority
                  )
                  if not (privileged or moderator) or (row["kind"] == "personal" and target == row["owner_id"]) or protected:
                      raise RoomError("moderation_forbidden", "This member cannot be removed", 403)
                  data["members"] = [m for m in data["members"] if m["user_id"] != target]
                  if target not in data["kicked"]:
                      data["kicked"].append(target)
                  data["pending_reentry"] = [x for x in data["pending_reentry"] if x != target]
              elif action == "moderator":
                  if not personal_owner:
                      raise RoomError("moderation_forbidden", "Only the personal room owner can delegate", 403)
                  target = self._target(payload)
                  found = next((m for m in data["members"] if m["user_id"] == target), None)
                  if not found or target == row["owner_id"]:
                      raise RoomError("member_not_found", "Member was not found", 404)
                  found["role"] = "moderator" if bool(payload.get("enabled")) else "participant"
              elif action == "request_reentry":
                  if uid not in data["kicked"]:
                      raise RoomError("reentry_not_required", "Re-entry approval is not required")
                  if uid not in data["pending_reentry"]:
                      data["pending_reentry"].append(uid)
                      notify = (row["owner_id"], row["id"], uid)
              elif action == "approve_reentry":
                  if not privileged:
                      raise RoomError("owner_required", "Only the room authority can approve re-entry", 403)
                  target = self._target(payload)
                  if target not in data["pending_reentry"]:
                      raise RoomError("request_not_found", "No pending re-entry request", 404)
                  data["pending_reentry"].remove(target)
                  data["kicked"] = [x for x in data["kicked"] if x != target]
                  if target not in data["invited"]:
                      data["invited"].append(target)
              elif action == "leave":
                  if personal_owner:
                      data["force_state"] = "ended"
                  else:
                      data["members"] = [m for m in data["members"] if m["user_id"] != uid]
              elif action == "close":
                  if not privileged:
                      code = "group_authority_required" if row["kind"] == "group" else "owner_required"
                      raise RoomError(code, "Room authority is required", 403)
                  data["force_state"] = "ended"
              elif action == "appearance":
                  for key in ("avatar", "outfit"):
                      if key in payload:
                          if not isinstance(payload[key], str) or len(payload[key]) > 64:
                              raise RoomError("invalid_appearance", f"{key} must be a short string")
                          actor[key] = payload[key]
              else:
                  raise RoomError("unknown_action", "Unsupported room action")
              return True, notify, playback_call'''
          )

          section(
              "    def _snapshot(",
              "    @staticmethod\n    def _new_data(",
              '''    async def _snapshot_for(self, row, user_id: int | None) -> dict:
              group_authority = False
              if row["kind"] == "group" and user_id is not None:
                  group_authority = await self._check(self.authority, user_id, row["chat_id"])
              return self._snapshot(row, user_id, group_authority)

          def _snapshot(self, row, user_id: int | None, group_authority: bool = False) -> dict:
              data, pb = self._data(row), self._timeline(self._data(row)["playback"])
              role = next((m["role"] for m in data["members"] if m["user_id"] == user_id), None)
              personal_owner = row["kind"] == "personal" and user_id == row["owner_id"]
              authority = group_authority if row["kind"] == "group" else personal_owner
              settings = dict(data["settings"])
              settings["password_required"] = bool(data["password_hash"])
              expires = data.get("expires_override", row["expires_at"])
              state = data.get("force_state", row["state"])
              can_shared = authority or role == "moderator"
              if row["kind"] == "personal" and settings["owner_lock"] and not personal_owner:
                  can_shared = False
              return {
                  "id": row["id"], "kind": row["kind"], "owner_id": row["owner_id"],
                  "chat_id": row["chat_id"], "revision": row["revision"], "state": state,
                  "expires_at": expires, "settings": settings,
                  "members": [{k: v for k, v in m.items() if v is not None} for m in data["members"]],
                  "pending_reentry": list(data["pending_reentry"]) if authority else [],
                  "playback": pb,
                  "permissions": {
                      "manage_settings": authority,
                      "queue": can_shared or settings["queue_all"],
                      "control": can_shared or (settings["queue_all"] and not settings["owner_lock"]),
                      "moderate": can_shared,
                      "close": authority,
                  },
              }'''
          )

          old = '''    def _commit_action(self, room_id, revision, data, user_id, action_id, digest, output, changed):
              with self._connect() as db:
                  if changed:
                      db.execute(
                          "UPDATE rooms SET revision=?,data_json=?,state=COALESCE(?,state) WHERE id=?",
                          (
                              revision,
                              json.dumps(data, separators=(",", ":")),
                              data.get("force_state"),
                              room_id,
                          ),
                      )
                  db.execute(
                      "INSERT INTO room_actions VALUES(?,?,?,?,?,?)",
                      (
                          room_id,
                          user_id,
                          action_id,
                          digest,
                          json.dumps(output, separators=(",", ":")),
                          time.time(),
                      ),
                  )'''
          new = '''    def _commit_action(
              self, room_id, revision, data, user_id, action_id, digest, output, changed, state
          ):
              with self._connect() as db:
                  if changed:
                      db.execute(
                          "UPDATE rooms SET revision=?,data_json=?,state=?,end_reason=? WHERE id=?",
                          (
                              revision,
                              json.dumps(data, separators=(",", ":")),
                              state,
                              None if state == "active" else data.get("end_reason"),
                              room_id,
                          ),
                      )
                  db.execute(
                      "INSERT INTO room_actions VALUES(?,?,?,?,?,?)",
                      (
                          room_id,
                          user_id,
                          action_id,
                          digest,
                          json.dumps(output, separators=(",", ":")),
                          time.time(),
                      ),
                  )'''
          if old not in text:
              raise SystemExit("commit action block did not match")
          text = text.replace(old, new, 1)

          marker = "    def _expire_due(self):"
          helpers = '''    def _save_status(self, room_id, revision, data, state, reason):
              with self._connect() as db:
                  db.execute(
                      "UPDATE rooms SET revision=?,data_json=?,state=?,end_reason=? WHERE id=?",
                      (revision, json.dumps(data, separators=(",", ":")), state, reason, room_id),
                  )

          @staticmethod
          def _playback_anchor(playback):
              return {
                  "track": playback.get("track"),
                  "status": playback.get("status"),
                  "position_seconds": playback.get("position_seconds", 0),
                  "queue": playback.get("queue", []),
              }

          '''
          text = text.replace(marker, helpers + marker, 1)

          old_replace = '''    @staticmethod
              def _replace(row, revision, data):
                  result = dict(row)
                  result["revision"] = revision
                  result["data_json"] = json.dumps(data)
                  if data.get("force_state"):
                      result["state"] = data["force_state"]
                  return result'''
          new_replace = '''    @staticmethod
              def _replace(row, revision, data, state=None):
                  result = dict(row)
                  result["revision"] = revision
                  result["data_json"] = json.dumps(data)
                  if state is not None:
                      result["state"] = state
                  elif data.get("force_state"):
                      result["state"] = data["force_state"]
                  return result'''
          if old_replace not in text:
              raise SystemExit("replace helper did not match")
          text = text.replace(old_replace, new_replace, 1)
          path.write_text(text)

          tests = Path("tests/test_rooms_service.py")
          test_text = tests.read_text()
          addition = r'''

          @pytest.mark.asyncio
          async def test_group_playback_failure_is_retryable_without_revision_change(tmp_path):
              attempts = 0

              async def playback(chat_id, action, payload):
                  nonlocal attempts
                  attempts += 1
                  if attempts == 1:
                      raise RuntimeError("voice command failed")
                  return {
                      "track": payload["track"],
                      "status": "playing",
                      "position_seconds": 0,
                      "queue": [],
                  }

              service = RoomService(
                  tmp_path / "rooms.db",
                  playback=playback,
                  authority=lambda uid, chat: uid == 9,
                  member=lambda uid, chat: uid == 9,
                  search=search,
              )
              room = await service.ensure_group(100, 1, 9)
              revision = room["revision"]
              with pytest.raises(RuntimeError, match="voice command failed"):
                  await service.action(room["id"], 9, "play", revision, "force_play", {"query": "One"})
              assert (await service.snapshot(room["id"], 9))["revision"] == revision
              result = await service.action(
                  room["id"], 9, "play", revision, "force_play", {"query": "One"}
              )
              assert attempts == 2
              assert result["playback"]["track"]["title"] == "One"


          @pytest.mark.asyncio
          async def test_group_playback_actions_are_serialized_once(tmp_path):
              active = 0
              maximum = 0
              calls = []

              async def playback(chat_id, action, payload):
                  nonlocal active, maximum
                  active += 1
                  maximum = max(maximum, active)
                  calls.append(payload["track"]["title"])
                  await __import__("asyncio").sleep(0.02)
                  active -= 1
                  return {
                      "track": payload["track"], "status": "playing",
                      "position_seconds": 0, "queue": [],
                  }

              service = RoomService(
                  tmp_path / "rooms.db", playback=playback,
                  authority=lambda uid, chat: True, member=lambda uid, chat: True, search=search,
              )
              room = await service.ensure_group(100, 1, 9)
              first = __import__("asyncio").create_task(
                  service.action(room["id"], 9, "one", room["revision"], "force_play", {"query": "One"})
              )
              await __import__("asyncio").sleep(0)
              with pytest.raises(RoomError, match="Refresh"):
                  await service.action(
                      room["id"], 9, "two", room["revision"], "force_play", {"query": "Two"}
                  )
              await first
              assert maximum == 1
              assert calls == ["One"]


          @pytest.mark.asyncio
          async def test_replay_rechecks_kick_and_expiry(tmp_path):
              service = RoomService(tmp_path / "rooms.db")
              room = await service.create_personal(1, 2)
              room = await service.join(room["id"], user(2))
              replayed = await service.action(
                  room["id"], 2, "look", room["revision"], "appearance", {"avatar": "cat"}
              )
              owner = await service.snapshot(room["id"], 1)
              await service.action(
                  room["id"], 1, "kick-replay", owner["revision"], "kick", {"user_id": 2}
              )
              with pytest.raises(RoomError, match="not active"):
                  await service.action(
                      room["id"], 2, "look", room["revision"], "appearance", {"avatar": "cat"}
                  )

              expiring = await service.create_personal(3, duration=300)
              historical = await service.action(
                  expiring["id"], 3, "saved", expiring["revision"], "appearance", {"avatar": "fox"}
              )
              with service._connect() as db:
                  db.execute("UPDATE rooms SET expires_at=? WHERE id=?", (time.time() - 1, expiring["id"]))
              with pytest.raises(RoomError, match="expired"):
                  await service.action(
                      expiring["id"], 3, "saved", expiring["revision"], "appearance", {"avatar": "fox"}
                  )
              assert replayed["id"] == room["id"] and historical["id"] == expiring["id"]


          @pytest.mark.asyncio
          async def test_group_recovery_and_live_authority_permissions(tmp_path):
              authorities = {9}
              service = RoomService(
                  tmp_path / "rooms.db",
                  authority=lambda uid, chat: uid in authorities,
                  member=lambda uid, chat: uid in {9, 10},
              )
              room = await service.ensure_group(100, 1, 9)
              joined = await service.join(room["id"], user(10))
              assert joined["permissions"]["manage_settings"] is False
              authorities.clear()
              authorities.add(10)
              owner_view = await service.snapshot(room["id"], 9)
              authority_view = await service.snapshot(room["id"], 10)
              assert owner_view["permissions"]["manage_settings"] is False
              assert authority_view["permissions"]["manage_settings"] is True
              await service.set_recovering(100, "transient voice failure")
              recovering = await service.snapshot(room["id"], 10)
              assert recovering["state"] == "recovering"
              before = recovering["revision"]
              await service.publish_playback(
                  100, {"track": TRACK, "status": "playing", "position_seconds": 0, "queue": []}
              )
              active = await service.snapshot(room["id"], 10)
              assert active["state"] == "active" and active["revision"] == before + 1
              await service.publish_playback(
                  100, {"track": TRACK, "status": "playing", "position_seconds": 0, "queue": []}
              )
              assert (await service.snapshot(room["id"], 10))["revision"] == active["revision"]
          '''
          if "test_group_playback_failure_is_retryable_without_revision_change" not in test_text:
              tests.write_text(test_text + addition)

          docs = Path("docs/rooms-api.md")
          doc = docs.read_text()
          doc = doc.replace(
              "The bot alone calls `create_personal`, `ensure_group`, `end_group`, and `publish_playback`.",
              "The bot alone calls `create_personal`, `ensure_group`, `set_recovering`, `end_group`, and `publish_playback`.",
          )
          doc = doc.replace(
              "`playback(chat_id, action, payload)` is async and can return the current playback snapshot.",
              "`playback(chat_id, action, payload)` is async and must return the actual registry playback snapshot after a successful group command. The service awaits it while holding the room action lock and commits only after success. The callback must call the media registry command only and must not call `publish_playback` itself. Failed commands remain retryable with the same action ID. Group playback accepts only supported HTTPS YouTube sources.",
          )
          doc = doc.replace(
              "Group authority is independently verified for settings and close.",
              "Group membership is rechecked for every action and replay. Group authority is independently verified for settings, close, moderation, and other privileged controls. Stored room ownership does not grant departed group owners authority. Call `set_recovering(chat_id, reason)` for transient media failures; a later successful command or `publish_playback` marks the room active. Duplicate observer playback snapshots with equal track, status, position, and queue anchors do not increment the revision.",
          )
          docs.write_text(doc)
          PY
      - name: Install project
        run: |
          python -m pip install --upgrade pip -q
          pip install -e '.[dev]' -q
      - name: Format and lint fix
        run: |
          ruff check --fix src/parlay/rooms/service.py tests/test_rooms_service.py
          ruff format src/parlay/rooms/service.py tests/test_rooms_service.py
      - name: Scoped service tests
        run: pytest -q tests/test_rooms_service.py
      - name: Full tests
        run: pytest -q
      - name: Type check
        run: mypy
      - name: Commit scoped result and remove runner
        run: |
          git config user.name "Alex C11"
          git config user.email "alex.c992954@gmail.com"
          git rm .github/workflows/rooms-authority-fix.yml
          git add src/parlay/rooms/service.py tests/test_rooms_service.py docs/rooms-api.md
          git commit -m "fix(rooms): serialize authoritative group playback"
          git push
